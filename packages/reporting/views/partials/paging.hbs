<div class="paging">
    <a href="#">&laquo;</a>
    <a href="{{first.url}}">{{first.name}}</a>
    {{#each pages}}<a href="{{this.url}}">{{this.name}}</a> {{/each}}
    {{#if last}}<a href="{{last.url}}">{{last.name}}</a> {{/if}}
    <a href="#">&raquo;</a>
</div>