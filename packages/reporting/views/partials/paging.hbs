<div class="paging">
    <a href="{{first.url}}">{{first.name}}</a>
    {{#if previous}}<a href="{{previous.url}}">«</a>{{/if}}
    {{#each pages}}<a href="{{this.url}}" {{#if current}}class="active"{{/if}}>{{this.name}}</a> {{/each}}
    {{#if last}}<a href="{{last.url}}">{{last.name}}</a> {{/if}}
    <a href="{{next.url}}">&raquo;</a>
</div>