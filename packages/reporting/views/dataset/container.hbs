{{#with dataset}}
<div {{datasetID this.name}} class="block" >
    <div class='container {{this.options.class}}' style="{{this.options.class}}">
        {{position "block" }}
    </div>
</div>
{{/with}}